{
  "name": "tillerquest",
  "version": "1.0.0",
  "description": "Tillerquest",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "generated/prisma"
  ],
  "scripts": {
    "lint": "eslint .",
    "commitlint": "commitlint --edit",
    "prepare": "husky",
    "dev": "concurrently --names \"backend,frontend\" \"pnpm --filter ./backend run start\" \"pnpm --filter ./frontend run dev\"",
    "start": "concurrently --names \"backend,frontend\" \"pnpm --filter ./backend run start\" \"pnpm --filter ./frontend run start\"",
    "gen": "cd backend && pnpm run gen",
    "reset": "cd backend && pnpm run reset"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "dotenv": "^17.4.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.3",
    "@commitlint/config-conventional": "^20.5.3",
    "@types/node": "^25.9.5",
    "@types/pg": "^8.20.2",
    "commitlint": "^20.5.3",
    "concurrently": "^10.0.4",
    "eslint": "^9.39.5",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.9.6",
    "prisma": "^7.9.1"
  }
}
